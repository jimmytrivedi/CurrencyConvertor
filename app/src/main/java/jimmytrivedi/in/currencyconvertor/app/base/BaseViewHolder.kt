package jimmytrivedi.`in`.currencyconvertor.app.base

import android.view.View
import androidx.recyclerview.widget.RecyclerView

/**
 * This is a Base class which provides Recyclerview-ViewHolder class functionality for entire project
 */
abstract class BaseViewHolder(private val view: View) : RecyclerView.ViewHolder(view) {

    /** --------------------------- overridden & default functions --------------------------- **/
}